# DO NOT commit real secrets. Replace CHANGE_ME values or use
# external-secrets/sealed-secrets to manage sensitive data.
#
# To generate a base64 placeholder:
#   echo -n 'your-actual-value' | base64
#
# Example with Sealed Secrets:
#   kubeseal --format yaml < secret.yaml > sealed-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: smart-talk-secrets
  namespace: smart-talk
type: Opaque
data:
  # HA Long-Lived Access Token (HA → Profile → Long-Lived Access Tokens)
  ha-token: Q0hBTkdFX01F

  # LLM API key (use "sk-no-key" for local Ollama deployments)
  llm-api-key: Q0hBTkdFX01F
