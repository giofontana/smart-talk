[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
filterwarnings = [
    "ignore::DeprecationWarning:websockets.connection",
]

[tool.coverage.run]
source = ["app"]
omit = ["tests/*"]
